/**
 * Object which stores all action types used in this application
 * @type {object}
 */
const ActionTypes = {
    SET_STREAM: 'SET_STREAM',
    SET_SOURCE: 'SET_SOURCE',
    SET_MEDIA: 'SET_MEDIA',
    START_RECORDING: 'START_RECORDING',
    STOP_RECORDING: 'STOP_RECORDING'
};

export default ActionTypes;
